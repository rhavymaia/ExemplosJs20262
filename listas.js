let numeros = [1, 4, 8];
for (let numero of numeros) {
}

numeros.forEach(() => {}); // sem retorno.
numeros.map(() => {}); // Retorna uma nova lista com a mesma quantidade de itens do original.
numeros.filter(() => {});
numeros.reduce(() => {}, 0);
